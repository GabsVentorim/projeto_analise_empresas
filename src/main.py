def main():

    # == RUM DEFINITION == 

    # get company names (file or database -> not sure each one will be better)

    # get the historical bias based on the code [internal] -> database

    # get infos + news about the companies using the web scraper [external] -> do the news (prob .json) need to be save? (i dont think so)

    # IA work to resume and give a rate using the external infos -> local vars

    # combine [ (internal + external) <-- result ] -> local vars

    # apply result to generate rating -> database
    
    # use the result + the previous bias to calculate a next bias for the next runs -> database
    
    # RESULT:
    # id_company, rating_position, text_about_company, date -> database
    # table MUST have date as an index!!!!!!

    print("Hello World!")

if __name__ == "__main__":
    main()
